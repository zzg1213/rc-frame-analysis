# Examples

This directory contains a small sample generated with:

```powershell
$env:PYTHONPATH = "$PWD\src"
& "C:\Project\anaconda3\envs\torch_cuda\python.exe" -m rc_frame_analysis.pipeline --layout 1 --n 1 --seed 42 --outdir out
```

Large generated datasets and plots are intentionally excluded from Git.
